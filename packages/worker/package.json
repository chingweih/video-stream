{
  "name": "@video-stream/worker",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build ./src/index.ts --compile --outfile out/server && rm -f .*.bun-build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@video-stream/shared": "workspace:*",
    "bullmq": "^5.67.2"
  }
}
