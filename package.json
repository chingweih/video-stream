{
  "name": "video-stream",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format:fix": "prettier . --write",
    "format:check": "prettier . -c"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
